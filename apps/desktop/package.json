{
  "name": "@eskander/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && concurrently -k \"tsc -w\" \"wait-on http://localhost:3000 && electron .\"",
    "build": "tsc"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "concurrently": "^9.2.1",
    "electron": "^43.0.0",
    "typescript": "^5.9.0",
    "wait-on": "^8.0.0"
  }
}
